// Keep the existing teacher routes while using the coverage-aware classic UI.
// The shared dashboard preserves the purple tabs and requires complete coverage
// before displaying the leave submission form.
export { default } from './WorkflowDashboard';
